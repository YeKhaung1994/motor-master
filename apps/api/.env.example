# --- Database (PostgreSQL) ---
DB_HOST=localhost
DB_PORT=5432
DB_NAME=motor_master
DB_USER=motor_master
DB_PASSWORD=motor_master_dev
# Managed providers (Neon, Supabase, RDS) require TLS; a local container does not.
DB_SSL=false

# --- API ---
PORT=4000
WEB_ORIGIN=http://localhost:5173
LOG_LEVEL=info

# --- Web ---
VITE_API_URL=/api/v1
